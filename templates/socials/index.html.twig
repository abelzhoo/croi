{% extends 'base.html.twig' %}

{% block title %}Departement Sante{% endblock %}


{% block content %}
<div id="content">
    <div id="content-header">
        <div id="breadcrumb"> <a href="#" title="Go to Home" class="tip-bottom"><i class="icon-home"></i> Accueil</a> <a href="#" class="current">Social</a> </div>
            <h1>DEPARTEMENT SOCIAL</h1>
        </div>
        <div class="container-fluid">
        <hr>
        <div class="row-fluid">
            <div class="span12">
                <div class="widget-box">
                    <div class="widget-title"> <span class="icon"><i class="icon-th"></i></span>
                        <h5 class="pull-left">social</h5>
                    </div>
                    <div class="widget-content nopadding">
                        <table class="table table-bordered data-table">
                            <thead>
                                <tr>
                                    <th>Id</th>
                                    <th>AideNourriture</th>
                                    <th>AideEducation</th>
                                    <th>AideSocial</th>
                                    <th>AideSante</th>
                                    <th>AideTravail</th>
                                    <th>actions</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for social in socials %}
                                <tr>
                                    <td>{{ social.id }}</td>
                                    <td>{{ social.aideNourriture}}</td>
                                    <td>{{ social.aideEducation}}</td>
                                    <td>{{ social.aideSocial}}</td>
                                    <td>{{ social.aideSante }}</td>
                                    <td>{{ social.aideTravail}}</td>
                                    <td>
                                        <a href="{{ path('app_dashboard_social_show', {'id': social.id}) }}" data-toggle="modal" data-target="#popUpWindow"><i class="icon-eye-open"></i></a>
                                        <a href="{{ path('app_dashboard_social_edit', {'id': social.id}) }}"><i class="icon-edit"></i></a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="7">Pas de donnée</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
<script src="{{ asset('js/jquery.min.js') }}"></script> 
<script src="{{ asset('js/matrix.tables.js') }}"></script>
{% endblock %}

{% block modal %}
    {{ include('socials/_modal.html.twig') }}
{% endblock %}